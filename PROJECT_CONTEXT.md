# Контекст проекта: Вращающиеся фигуры (мультиплеер)

## Цель проекта
Веб-приложение с интерактивными фигурами: при клике фигура поворачивается в
следующее положение (например, +90°), и это изменение видно в реальном
времени всем удалённо подключённым пользователям.

Это НЕ приложение внутри Miro — отдельный самостоятельный веб-сервис
(изначально рассматривали Miro Web SDK, но решили делать отдельно).

## Формат работы
Образовательный проект — Claude объясняет теорию и пишет код вместе с
пользователем, построчно, с разбором каждой части (а не просто выдаёт
готовый код).

## Уровень пользователя
- Python: сильный (numpy/расчёты/графики), но НЕТ опыта с веб-бэкендами
  (лёгкий опыт с FastAPI был давно, почти забыт)
- HTML/CSS/JS: с нуля, не знает вообще
- Инструменты: PyCharm (Settings/Preferences глючили — не открывались),
  венвы использует регулярно, работает в обычном cmd
- ОС: Windows

## Архитектура (согласовано)
```
Браузер (клиент) <--WebSocket--> Python-сервер (FastAPI) <--WebSocket--> Браузер (клиент)
```
Сервер хранит состояние (углы поворота фигур) как единый источник правды.
Клик в браузере -> сообщение по WebSocket серверу -> сервер обновляет
состояние -> рассылает всем подключённым клиентам -> все видят синхронно.

## Структура проекта
```
Индивидуальное приложение/
├── .venv/                  (venv, в git не попадает)
├── .git/                   (репозиторий инициализирован и связан с GitHub)
├── app/
│   ├── main.py             (ещё не создан — следующий шаг)
│   └── static/
│       └── index.html      (ещё не создан)
├── requirements.txt        (fastapi==0.115.0, uvicorn[standard]==0.32.0)
├── .gitignore               (.venv/, __pycache__/, *.pyc, .idea/, .obsidian/, *.log)
├── PROJECT_CONTEXT.md       (этот файл)
├── Занятие 1.md             (личная заметка пользователя, не код проекта)
└── .idea/, .obsidian/       (служебные, PyCharm и Obsidian, в git не попадают)
```

Путь к проекту на диске:
`D:\ДНД\Приложения_для_ДНД\Индивидуальное приложение`

Папка также используется как Obsidian-vault для личных заметок пользователя.

## GitHub
Репозиторий: https://github.com/AntonVasin26/DnD_app
Remote: `origin` -> `git@github.com:AntonVasin26/DnD_app.git`
Аутентификация: SSH-ключ (ed25519), добавлен в GitHub аккаунт.

ВАЖНО (техническая деталь на будущее): встроенный SSH-клиент Git for
Windows ломался на кириллице в пути пользователя (`C:\Users\Антон`).
Исправлено глобальной настройкой git, использующей системный OpenSSH:
```
git config --global core.sshCommand "C:/Windows/System32/OpenSSH/ssh.exe"
```
Эта настройка глобальная (действует для всех репозиториев на этой машине),
если будут проблемы с push/pull/fetch по SSH в других проектах — вспомнить
про это.

## Что уже сделано
1. Создана структура папок (app/, app/static/)
2. Создан requirements.txt (fastapi==0.115.0, uvicorn[standard]==0.32.0)
3. Решена проблема с окружением: системные команды `python`/`py` были
   сломаны (заглушка Microsoft Store / битый путь в py.ini). Найден рабочий
   интерпретатор по пути:
   `D:\Programm_for_work\python\python_interp_3.12.10\python.exe`
   Venv создан вручную командой:
   `"D:\Programm_for_work\python\python_interp_3.12.10\python.exe" -m venv .venv`
   Активация: `.venv\Scripts\activate.bat`
4. Зависимости установлены (`pip install -r requirements.txt`)
5. Создан .gitignore (исключает .venv/, __pycache__/, .idea/, .obsidian/,
   *.pyc, *.log)
6. Git настроен полностью:
   - `git init`, первый коммит сделан
   - Сгенерирован SSH-ключ (`ssh-keygen -t ed25519`), добавлен на GitHub
   - Исправлена проблема с кириллицей в SSH (см. раздел GitHub выше)
   - Репозиторий создан на GitHub: github.com/AntonVasin26/DnD_app
   - `git remote add origin` + `git push -u origin master` — выполнены
     успешно, код уже на GitHub

## Известные проблемы окружения (решены, держим в памяти на будущее)
- PyCharm: Settings/Preferences не открывались (баг IDE) — обошли работой
  через обычный терминал (cmd); сама IDE для написания кода пока не
  использовалась в этом проекте.
- Системный `python`/`py` не работают напрямую — обходим, вызывая venv
  напрямую или указывая полный путь к интерпретатору при создании venv.
- Встроенный SSH в Git for Windows не работает с кириллицей в пути
  пользователя — исправлено через core.sshCommand (см. раздел GitHub).

## План дальнейших шагов (по порядку)
1. [ ] Написать минимальный `app/main.py` (FastAPI, один GET-эндпоинт
       "Hello world"), разобрать что такое ASGI/uvicorn/декораторы-роуты
2. [ ] Запустить сервер (`uvicorn app.main:app --reload`), проверить в
       браузере на localhost
3. [ ] Создать `app/static/index.html` со статичной фигурой (SVG),
       параллельно объяснять базовый HTML/CSS (пользователь не знает
       HTML/CSS/JS вообще — эту часть объяснять подробно, не торопясь)
4. [ ] Научить FastAPI отдавать этот HTML-файл
5. [ ] Добавить JS: клик по фигуре -> поворот через CSS transform
       (сначала БЕЗ синхронизации, просто локально, чтобы понять JS-события)
6. [ ] Объяснить теорию WebSocket подробнее
7. [ ] Добавить WebSocket-эндпоинт на сервере (принимать клики, хранить
       состояние углов, рассылать broadcast всем подключённым клиентам)
8. [ ] Добавить WebSocket-логику на клиенте (подключение, отправка клика,
       получение обновлений от сервера, обновление DOM)
9. [ ] Протестировать с нескольких вкладок/устройств одновременно
10. [ ] (Опционально, позже) деплой на внешний хостинг (Render/Railway/
        Fly.io и т.п.), чтобы доступ был не только в локальной сети
11. [ ] Не забывать делать `git add` / `git commit` / `git push` после
        каждого значимого шага (пока делали это только вручную по просьбе)

## Как продолжить в новом чате
Просто скажи Claude: "прочитай PROJECT_CONTEXT.md в папке
D:\ДНД\Приложения_для_ДНД\Индивидуальное приложение и продолжим".
Не забудь после каждого значимого шага просить Claude обновлять этот файл
(разделы "Что уже сделано" и "План дальнейших шагов").
