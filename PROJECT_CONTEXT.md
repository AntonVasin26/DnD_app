# Контекст проекта: Вращающаяся математическая головоломка (мультиплеер)

## Цель проекта (уточнено)
Это НЕ просто "клик -> поворот фигуры". Реальная концепция:

Головоломка из нескольких равносторонних многогранников (треугольники,
квадраты и т.д.). У соседних фигур есть ОБЩИЕ вершины (точки). В вершинах
стоят числа. При клике фигура физически поворачивается на 1 шаг (как
жёсткое тело — рамка + вершины), и числа в её вершинах циклически
сдвигаются — включая общие вершины с соседними фигурами, что связывает
фигуры между собой.

Есть "линии" — произвольные наборы точек (могут проходить через
несколько фигур сразу), для которых нужно добиться математического
условия (сейчас: сумма = цель). Игрок должен покрутить фигуры так, чтобы
ВСЕ линии одновременно выполняли своё условие — это и есть решение уровня.

Будет НЕСКОЛЬКО уровней (жёстко заданных заранее, не случайная генерация).
Ещё не решено: показывать все уровни на одном экране или отдельный экран
выбора уровня — решим на этапе фронтенда, на бэкенд-архитектуру не влияет.

Мультиплеер: любой подключённый игрок может крутить любую фигуру в любой
момент — совместное решение ОДНОЙ головоломки, все видят изменения в
реальном времени (через WebSocket, синхронизация состояния сервером).
Уже реализовано на фронтенд-прототипе: пока крутится одна фигура, клик по
другой (или по той же) игнорируется до конца анимации (глобальный флаг
isAnimating) — предвосхищает будущую многопользовательскую блокировку.

Это НЕ приложение внутри Miro — отдельный самостоятельный веб-сервис.

## Формат работы
Образовательный проект — Claude объясняет теорию и пишет код вместе с
пользователем, построчно, с разбором каждой части (а не просто выдаёт
готовый код).

## Уровень пользователя
- Python: сильный (numpy/расчёты/графики), но НЕТ опыта с веб-бэкендами
  (лёгкий опыт с FastAPI был давно, почти забыт). С Pydantic (BaseModel)
  познакомился в рамках этого проекта.
- HTML/CSS/JS: с нуля — теперь есть заметный практический опыт (SVG,
  CSS transitions/transform/filter/градиенты, JS DOM/события/setTimeout-
  цепочки/чистые функции) через глубокую проработку анимации поворота и
  визуальных эффектов (см. ниже — там было много итераций и разбора багов).
- Инструменты: PyCharm (переустановлен, обычная Community-версия), венвы
  использует регулярно, работает и в cmd, и в PowerShell
- ОС: Windows

## Архитектура (согласовано)
```
Браузер (клиент) <--WebSocket--> Python-сервер (FastAPI) <--WebSocket--> Браузер (клиент)
```
Сервер хранит состояние (текущие значения точек) как единый источник
правды. Клик по фигуре в браузере -> сообщение по WebSocket серверу ->
сервер поворачивает фигуру в PuzzleState -> рассылает всем подключённым
клиентам новое состояние -> все видят синхронно.

ТЕКУЩИЙ СТАТУС: прототип интерфейса (app/static/index.html) пока работает
ПОЛНОСТЬЮ АВТОНОМНО на чистом JS, без связи с бэкендом (дублирует логику
rotate/сумм на клиенте вручную). WebSocket-подключение к реальному
PuzzleState с бэкенда — ещё предстоит сделать (см. план).

## Модель данных головоломки (бэкенд, реализовано и проверено)
Ключевая идея: точки существуют независимо от фигур. Фигуры и линии — это
просто СПИСКИ id точек, ссылающиеся на общий пул. Общие вершины между
фигурами получаются естественно: два Shape ссылаются на один и тот же
point_id.

- **Point** — id + value (число)
- **Shape** — id + point_ids
- **Line** — id + point_ids + operation (пока только "sum") + target
- **Level** — неизменный шаблон: id, name, points, shapes, lines
- **PuzzleState** — состояние КОНКРЕТНОЙ игровой сессии.
  `rotate(shape_id, direction)`, `is_solved()`.

ВАЖНО: backend test-файлы (test_engine_butterfly.py) используют старую
2-точечную версию диагоналей (A-E, B-D, target=6) — фронтенд-прототип
давно ушёл вперёд на 3-точечную версию (A-C-E, B-C-D, target=9). Нужно
синхронизировать при подключении реального WebSocket.

## Фронтенд-прототип (app/static/index.html) — АРХИТЕКТУРА АНИМАЦИИ
Это была самая сложная часть проекта — потребовалось несколько итераций,
чтобы прийти к надёжному решению. Финальная архитектура:

### Геометрия
Два равносторонних треугольника-бабочка, ГОРИЗОНТАЛЬНО (лежат на боку):
  A(120,150), D(120,330) — база левого треугольника (вершина смотрит вправо)
  B(432,150), E(432,330) — база правого треугольника (вершина смотрит влево)
  C(276,240) — общая вершина, ровно на пересечении обеих диагоналей
Центроиды (для поворота): left=(172,240), right=(380,240)

### КЛЮЧЕВОЙ АРХИТЕКТУРНЫЙ УРОК: текст должен быть НЕЗАВИСИМ от рамки
Изначально пробовали класть <text> ВНУТРЬ вращающейся SVG-группы (рамка +
кружки), с "противо-вращением" текста, чтобы цифры оставались читаемыми.
Это привело к тонкому, долго не находившемуся багу: элемент "text-left-A"
физически ПЕРЕМЕЩАЛСЯ вместе с родительской группой при повороте (т.к.
лежал внутри неё), поэтому когда мы писали в него values.A, оказывалось,
что на экране это значение появляется НЕ на месте A, а там, куда его
унёс поворот родителя — путаница между "именем" элемента и его реальным
положением на экране. Итоговое РЕШЕНИЕ: текст полностью ВЫНЕСЕН из
вращающихся групп, это независимые <text id="text-A">...</text> и т.д.
прямо в SVG, всего 5 штук (по одной на точку A/B/C/D/E — БЕЗ дублирования
для общей C, т.к. текст больше не привязан к конкретной фигуре). Они
никогда не вращаются и не двигаются — только их textContent обновляется.

### Слои вращающихся групп — рёбра и кружки РАЗДЕЛЕНЫ (z-order фикс)
Изначально каждая фигура была ОДНОЙ SVG-группой (рёбра+кружки вместе).
Проблема: правый треугольник целиком рисуется ПОСЛЕ левого, значит его
толстые рёбра могли перекрыть кружок/подсветку левого треугольника в
общей точке C. РЕШЕНИЕ: каждая фигура разбита на ДВЕ группы —
`shape-{id}-edges` (только линии-рёбра + кликабельный polygon-overlay) и
`shape-{id}-circles` (только кружки + подсветка). В разметке СНАЧАЛА идут
ОБЕ группы edges (обеих фигур), ЗАТЕМ ОБЕ группы circles — так кружки
любой фигуры гарантированно оказываются поверх рёбер любой другой.
`circles`-группы имеют класс `.circles-layer { pointer-events: none; }` —
иначе клик по кружку не долетал бы до кликабельного overlay в edges-слое
под ним. В JS обе группы (edges+circles) одной фигуры поворачиваются
СИНХРОННО через общую функцию `setShapeTransform(shapeId, transformStr)`.

### Расчёт направления сдвига чисел — через реальную геометрию, не вручную
Была вторая серия багов из-за попыток вручную угадать порядок точек
(по/против часовой стрелки) для формулы циклического сдвига — несколько
раз ошиблись с направлением. ИТОГОВОЕ РЕШЕНИЕ: функция rotatePoint(x, y,
cx, cy, angleDeg) — стандартная формула поворота точки (та же математика,
что и CSS transform rotate()). Вынесена в чистую функцию
`computeRotatedValues(shapeId, direction, sourceValues)` — принимает
состояние на вход, возвращает НОВОЕ состояние, не трогая глобальные
переменные. Используется дважды: для реального применения
(`applyDataRotation`, мутирует `values`) и для ПРЕДСКАЗАНИЯ результата
ДО начала анимации (см. ниже про подсветку диагоналей).
Подтверждённые (вычисленные) циклы при клике (direction=+1, по часовой):
  левый треугольник: A → C → D → A
  правый треугольник: B → E → C → B

### Вращение как "жёсткое тело" (рамка + кружки) — без сброса угла
`rotationState.left` / `.right` НАКАПЛИВАЮТСЯ бесконечно (никогда не
сбрасываются к 0) — на каждый клик += ±120°. Поворот строится функцией
rotationAroundPoint(px, py, angleDeg) — явная схема
`translate(px,py) rotate(angleDeg) translate(-px,-py)`, НЕ через CSS
`transform-origin` (тот способ давал баги при вложенных группах).
При загрузке страницы группы ИНИЦИАЛИЗИРУЮТСЯ этой же составной формулой
на угол 0 — иначе первый клик анимируется иначе, чем все последующие.

### Анимация клика — три последовательных этапа (setTimeout-цепочка)
0. ПРЕЖДЕ чем гасить цифры: вычисляем predictedValues через
   computeRotatedValues (НЕ мутируя values). Для каждой линии сравниваем
   "было верно / будет верно" — если диагональ СЕЙЧАС светится, а ПОСЛЕ
   поворота перестанет совпадать с целью — гасим её подсветку СРАЗУ,
   ещё до начала анимации (а не только в конце, когда это выглядело бы
   как резкий рывок).
1. Цифры этой фигуры ПЛАВНО гаснут (opacity 0, 0.25с).
2. После угасания — рамка физически поворачивается на 120° (1.6с).
3. После поворота — applyDataRotation() пересчитывает values, render()
   обновляет все текстовые/визуальные элементы, затем цифры плавно
   проявляются обратно (opacity 1, 0.25с).
Итоговая длительность одного клика: 250+1600+250 = 2100мс.
Глобальный флаг `isAnimating` блокирует ЛЮБЫЕ клики на всё время
последовательности.
ВАЖНО: длительности FADE_DURATION_MS / ROTATE_DURATION_MS в JS ДОЛЖНЫ
точно совпадать с соответствующими CSS transition.

### Визуальный стиль фигур (финальный)
- Рамки (линии) и кружки — один тёмно-синий цвет #16213e.
- Толщина линий рамки: 8px.
- Заливка треугольника (кликабельная область) — #16213e с прозрачностью
  0.85 в покое / 0.95 при наведении (почти сплошная).
- Текст цифр вершин — белый (#ffffff).
- Кружки имеют белую/тёплую ПОДСВЕТКУ (radialGradient "circle-glow" в
  <defs>): прозрачно внутри (закрыто самим кружком), яркая вспышка ровно
  на границе (r=20 из r=34 круга подсветки ≈ 59%), плавно гаснет наружу.
  Один переиспользуемый градиент на все кружки (objectBoundingBox).

### Подсветка диагоналей (цель/сумма) — ПОЛНОСТЬЮ ПЕРЕДЕЛАНО
СТАРЫЙ вариант (таблички "Цель: X / Сумма: Y" в прямоугольниках справа) —
ПОЛНОСТЬЮ УБРАН по просьбе пользователя. Новый вариант:
- Диагональ рисуется как ОТДЕЛЬНАЯ (не физическая, не рёбра треугольника)
  светящаяся `<polyline>` прямо через вершины A-C-E / B-C-D — НЕ имеет
  transform вообще, т.к. точки A/B/C/D/E физически никогда не двигаются
  (двигается/крутится только рамка вокруг них, не сами координаты).
  По умолчанию `opacity: 0` (невидима), класс `.diagonal-overlay-ok`
  делает её видимой — только когда сумма совпадает с целью.
- Линия состоит из ДВУХ частей (разный класс, оба переключаются вместе):
  `.diagonal-overlay` — основной сегмент между вершинами (stroke-width 6),
  `.diagonal-overlay-extension` — тонкое (0.6px!) продолжение ЗА крайнюю
  вершину (E для diag1, за B для diag2) до надписи с числами. Эта часть
  ещё и более тусклая (`opacity: 0.45` вместо 1 у основной части, менее
  выраженный glow).
- На конце продолжения — надпись `ratio-diag1` / `ratio-diag2`, формат
  "{текущая сумма}/{цель}" (например "9/9"), БЕЗ отдельной рамки/фона.
  Белый текст (#ffffff), крупный (30px), с тёмной обводкой (stroke) для
  читаемости на текстурном фоне. По умолчанию `opacity: 0.35` (тускло) —
  класс `.sum-ok` возвращает `opacity: 1` + добавляет тёплое свечение
  (drop-shadow), когда сумма верна.
- Координаты точек продолжения (за вершину, до надписи) вычислены
  вручную по направлению диагонали: diag1 продолжается за E(432,330) до
  (510,375); diag2 продолжается за B(432,150) до (510,105).

### Текстуры фона
Папка `app/static/textures/`. Файлы: floor.webp (не идеально бесшовный),
background.jpg (не использован), Tiles_Roughness.jpg (используется,
background-repeat + background-size: 512px 512px на области 853×480px).
ВАЖНЫЙ УРОК (решённый баг): FastAPI не раздавал файлы из static/ без
явного `app.mount("/", StaticFiles(directory="app/static"), name="static")`
в КОНЦЕ main.py (после всех @app.get(...)).
Обсуждали (не сделано): извлечение бесшовных текстур пола из паков
Dungeondraft — свериться с лицензией при публичном релизе.

## Структура проекта
```
Индивидуальное приложение/
├── .venv/                     (venv, в git не попадает)
├── .git/                      (репозиторий инициализирован и связан с GitHub)
├── app/
│   ├── main.py                 (FastAPI: GET "/", GET "/api/status",
│   │                             + app.mount("/", StaticFiles(...)) в конце)
│   ├── puzzle_models.py        (Pydantic-модели: Point, Shape, Line, Level)
│   ├── puzzle_engine.py        (PuzzleState: rotate(), is_solved())
│   └── static/
│       ├── index.html          (прототип интерфейса — см. подробный разбор
│       │                        архитектуры выше)
│       └── textures/
│           ├── floor.webp
│           ├── background.jpg
│           └── Tiles_Roughness.jpg  (текущая используемая текстура)
├── run.py                      (запуск сервера: `python run.py`)
├── test_engine.py              (ручной тест: 1 треугольник)
├── test_engine_butterfly.py    (ручной тест, СТАРАЯ 2-точечная версия
│                                 диагоналей — не совпадает с фронтендом)
├── requirements.txt            (fastapi==0.115.0, uvicorn[standard]==0.32.0)
├── .gitignore                   (.venv/, __pycache__/, *.pyc, .idea/, .obsidian/, *.log)
├── PROJECT_CONTEXT.md           (этот файл)
├── Занятие 1.md                 (личная заметка пользователя, не код проекта)
└── .idea/, .obsidian/           (служебные, PyCharm и Obsidian, в git не попадают)
```

Путь к проекту на диске:
`D:\ДНД\Приложения_для_ДНД\Индивидуальное приложение`

Папка также используется как Obsidian-vault для личных заметок пользователя.

## Как запустить сервер локально
```
.venv\Scripts\activate.bat        (cmd)  ИЛИ  .\.venv\Scripts\Activate.ps1  (PowerShell)
python run.py
```
Сервер стартует на http://127.0.0.1:8000

## Как запустить ручные тесты движка головоломки
```
python test_engine.py
python test_engine_butterfly.py
```

## Swagger UI
http://127.0.0.1:8000/docs, http://127.0.0.1:8000/redoc.
ВАЖНО: WebSocket-эндпоинты в Swagger UI НЕ отображаются.

## GitHub
Репозиторий: https://github.com/AntonVasin26/DnD_app
Remote: `origin` -> `git@github.com:AntonVasin26/DnD_app.git`
Аутентификация: SSH-ключ (ed25519).
ВАЖНО: встроенный SSH-клиент Git for Windows ломался на кириллице в пути
пользователя. Исправлено: `git config --global core.sshCommand
"C:/Windows/System32/OpenSSH/ssh.exe"`

## Известные технические нюансы (держим в памяти)
- Системный `python`/`py` не работают напрямую — вызываем venv напрямую.
- В PowerShell venv активируется отдельно от cmd.
- Пользователь иногда печатает не в той раскладке клавиатуры (например,
  "знсрфкь" = "pycharm" в ЙЦУКЕН).
- Файлы, добавленные вручную через проводник, часто с пробелами/кириллицей
  в имени — переименовываем в латиницу перед использованием в коде.
- FastAPI НЕ раздаёт файлы из static/ без явного app.mount(StaticFiles).
- При работе с CSS transition + JS setTimeout — длительности ОБЯЗАТЕЛЬНО
  дублировать константой в JS и держать в синхроне с CSS, иначе баги.
- SVG-элементы, вложенные в повёрнутую (CSS transform) группу, физически
  наследуют это перемещение — если что-то должно оставаться на фиксированном
  месте независимо от поворота родителя, его нужно вынести из этой группы,
  а не пытаться компенсировать поворотом на дочернем уровне.
- В SVG порядок отрисовки = порядок в документе (что позже — то выше по
  z-order). Если нужно гарантировать, что элементы категории А всегда
  выше элементов категории Б НЕЗАВИСИМО от того, к какой "логической
  группе" они относятся — их нужно физически разложить по отдельным
  слоям в правильном порядке, а не полагаться на группировку по смыслу.
- decorative/overlay-элементы, которые не должны перехватывать клики
  (кружки, подсветка, линии-диагонали) — всегда добавлять
  `pointer-events: none`.

## План дальнейших шагов (по порядку)
1. [x] Написать минимальный `app/main.py`
2. [x] Запустить сервер, проверить в браузере и через Swagger UI
3. [x] Спроектировать и реализовать модель данных головоломки (бэкенд)
4. [x] Реализовать движок (puzzle_engine.py: rotate/is_solved)
5. [x] Проверить механику вручную на бэкенде (тестовые скрипты)
6. [x] Создать HTML/CSS/JS-прототип интерфейса
7. [x] Настроить раздачу статики (StaticFiles)
8. [x] Подключить фоновую текстуру, подобрать размер области (853×480, 16:9)
9. [x] Реализовать полноценную анимацию физического поворота фигуры
       (рамка+кружки как жёсткое тело, текст независим, три фазы:
       гаснет/крутится/зажигается), включая блокировку параллельных
       поворотов и математически точный расчёт направления сдвига чисел
10. [x] Настроить визуальный стиль (единый цвет, толщина линий, заливка)
10.5. [x] Code review index.html на лишние/забытые элементы
11. [x] Добавить подсветку кружкам (radialGradient "circle-glow")
12. [x] Развести рёбра и кружки по отдельным слоям (z-order фикс, чтобы
        кружки/подсветка гарантированно были выше рёбер соседней фигуры)
13. [x] Переделать подсветку диагоналей: убрать таблички "Цель/Сумма",
        сделать светящуюся линию-накладку через вершины + продолжение за
        пределы фигуры к надписи "текущее/цель" (тонкое, тусклое
        продолжение отдельно от основной толстой яркой части)
14. [x] Предсказание результата ДО анимации — гасить подсветку диагонали
        заранее, если поворот её сломает, а не только в конце
15. [ ] Синхронизировать логику линий между бэкендом и фронтенд-прототипом
        (2-точечные vs 3-точечные диагонали)
16. [ ] Решить и реализовать хранение НЕСКОЛЬКИХ уровней на бэкенде
17. [ ] Добавить HTTP-маршруты: GET /levels, GET /levels/{id}
18. [ ] Объяснить теорию WebSocket подробно
19. [ ] Добавить WebSocket-эндпоинт на сервере: принимать клики (shape_id,
        direction), вызывать PuzzleState.rotate(), рассылать всем
        подключённым клиентам новое state.values + is_solved()
20. [ ] Переписать JS на фронтенде: убрать дублирующую локальную логику
        rotate/сумм, подключиться к WebSocket
21. [ ] Решить UI выбора уровня
22. [ ] Протестировать с нескольких вкладок/устройств одновременно
23. [ ] (Опционально) деплой на внешний хостинг
24. [ ] Периодически делать `git add` / `git commit` / `git push`

## Как продолжить в новом чате
Просто скажи Claude: "прочитай PROJECT_CONTEXT.md в папке
D:\ДНД\Приложения_для_ДНД\Индивидуальное приложение и продолжим".
Не забудь после каждого значимого шага просить Claude обновлять этот файл.
